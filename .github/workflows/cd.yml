# ---------------------------------------------------------------------------
# CD - Deploy to Azure
# ---------------------------------------------------------------------------
# Deploys Terraform infra on merge to main. dbt model execution is handled
# by the w3c_dbt_marts_azure Airflow DAG (dataset-triggered).
#
# Single environment: azure-dev (auto-approve on merge to main).
# Rollback available via workflow_dispatch.
#
# Pull requests run terraform-plan only (read-only). Apply is skipped on PRs
# — fast feedback without risking the environment.
# ---------------------------------------------------------------------------
name: CD - Deploy to Azure

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: cd-terraform  # Single slot — cancel stale runs to avoid state lock collisions
  cancel-in-progress: true

env:
  TF_VERSION: "1.10.5"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # -----------------------------------------------------------------------
  # Terraform Plan (read-only, always runs)
  # -----------------------------------------------------------------------
  terraform-plan:
    name: Terraform Plan
    # Dependabot PRs skip the plan entirely: Azure OIDC + state backend are
    # decommissioned, so any plan step that touches Azure will fail. The
    # dependabot auto-merge workflow handles terraform PRs (no real CD risk).
    if: github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    environment: azure-dev
    permissions:
      id-token: write
      contents: read
    outputs:
      plan_platform_exitcode: ${{ steps.plan_platform.outputs.exitcode }}
      plan_databricks_exitcode: ${{ steps.plan_databricks.outputs.exitcode }}
    env:
      ARM_USE_OIDC: true
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "${{ env.TF_VERSION }}"

      - name: Terraform Plan (Platform)
        id: plan_platform
        run: |
          cd terraform/platform
          terraform init -input=false
          set -o pipefail
          # -lock=false: safe for read-only plans, avoids state lock contention
          # when a concurrent apply holds the lock.
          terraform plan -no-color -lock=false -input=false -var-file=environments/dev/terraform.tfvars 2>&1 | tee tfplan_platform.log || exitcode=$?
          echo "exitcode=${exitcode:-0}" >> "$GITHUB_OUTPUT"

      - name: Upload Platform Plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-platform
          path: terraform/platform/tfplan_platform.log

      - name: Terraform Plan (Databricks)
        id: plan_databricks
        if: github.actor != 'dependabot[bot]'
        env:
          DATABRICKS_AUTH_TYPE: pat
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          TF_VAR_azure_sql_password: ${{ secrets.AZURE_SQL_PASSWORD }}
          TF_VAR_storage_access_key: ${{ secrets.STORAGE_ACCESS_KEY }}
        run: |
          cd terraform/databricks
          terraform init -input=false
          set -o pipefail
          terraform plan -no-color -input=false -var-file=environments/dev/terraform.tfvars 2>&1 | tee tfplan_databricks.log || exitcode=$?
          echo "exitcode=${exitcode:-0}" >> "$GITHUB_OUTPUT"

      - name: Upload Databricks Plan
        if: steps.plan_databricks.conclusion == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-databricks
          path: terraform/databricks/tfplan_databricks.log

  # -----------------------------------------------------------------------
  # Terraform Apply (per environment, gated by GitHub Environments)
  # -----------------------------------------------------------------------
  terraform-apply:
    name: Terraform Apply
    if: github.event_name != 'pull_request'
    needs: terraform-plan
    runs-on: ubuntu-latest
    environment: azure-dev
    permissions:
      id-token: write
      contents: read
    timeout-minutes: 30
    env:
      ARM_USE_OIDC: true
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "${{ env.TF_VERSION }}"

      - name: Terraform Apply (Platform)
        run: |
          cd terraform/platform
          terraform init -input=false -migrate-state
          terraform apply -auto-approve -input=false -lock-timeout=300s -var-file=environments/dev/terraform.tfvars

      - name: Terraform Apply (Databricks)
        env:
          DATABRICKS_AUTH_TYPE: pat
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          TF_VAR_azure_sql_password: ${{ secrets.AZURE_SQL_PASSWORD }}
          TF_VAR_storage_access_key: ${{ secrets.STORAGE_ACCESS_KEY }}
        run: |
          cd terraform/databricks
          terraform init -input=false
          terraform apply -auto-approve -input=false -lock-timeout=300s -var-file=environments/dev/terraform.tfvars

  # -----------------------------------------------------------------------
  # Rollback (manual workflow_dispatch only)
  # -----------------------------------------------------------------------
  rollback:
    name: Rollback
    if: github.event_name == 'workflow_dispatch'
    needs: [terraform-plan]
    runs-on: ubuntu-latest
    environment: azure-dev
    permissions:
      id-token: write
      contents: read
    env:
      ARM_USE_OIDC: true
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history needed to resolve HEAD~1

      - name: Checkout previous commit (rollback)
        run: git checkout HEAD~1

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "${{ env.TF_VERSION }}"

      - name: Terraform Rollback — Platform (plan)
        run: |
          cd terraform/platform
          terraform init -input=false -migrate-state
          terraform plan -no-color -input=false -var-file=environments/dev/terraform.tfvars -out=tfplan_rollback

      - name: Terraform Rollback — Platform (apply)
        run: |
          cd terraform/platform
          terraform apply -auto-approve -input=false -lock-timeout=300s tfplan_rollback

      - name: Terraform Rollback — Databricks (plan)
        env:
          DATABRICKS_AUTH_TYPE: pat
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          TF_VAR_azure_sql_password: ${{ secrets.AZURE_SQL_PASSWORD }}
          TF_VAR_storage_access_key: ${{ secrets.STORAGE_ACCESS_KEY }}
        run: |
          cd terraform/databricks
          terraform init -input=false -migrate-state
          terraform plan -no-color -input=false -var-file=environments/dev/terraform.tfvars -out=tfplan_rollback

      - name: Terraform Rollback — Databricks (apply)
        env:
          DATABRICKS_AUTH_TYPE: pat
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          TF_VAR_azure_sql_password: ${{ secrets.AZURE_SQL_PASSWORD }}
          TF_VAR_storage_access_key: ${{ secrets.STORAGE_ACCESS_KEY }}
        run: |
          cd terraform/databricks
          terraform init -input=false
          terraform apply -auto-approve -input=false -lock-timeout=300s tfplan_rollback


