user-agents>=2.2,<3.0
requests
azure-storage-blob>=12.24.0
dbt-postgres==1.8.2
dbt-core==1.8.9
dbt-sqlserver==1.8.7
psycopg2-binary>=2.9,<3.0
pyodbc>=5.3.0,<6.0
sqlalchemy>=2.0,<3.0
# Pin protobuf to 5.27+: the `always_print_fields_with_no_presence` kwarg
# used by dbt-common 1.27.1 (MessageToJson) was added in protobuf 5.27.
# Airflow 2.10 base image ships protobuf 4.25.9; must be upgraded or
# dbt run/test/docs all crash with TypeError on the final Resource report.
protobuf>=5.27,<6
# dbt-core 1.8.x is built against protobuf 5; dbt-common 1.28+ moves to protobuf 6.
dbt-common==1.27.1

# Data freshness probe dependencies
prometheus-client>=0.21.0,<1.0
pymssql>=2.3.0,<3.0

# Data lineage — OpenLineage events emitted to self-hosted Marquez
# (see airflow/lineage/docker-compose.lineage.yaml).
# Provider capped <2: 2.x requires apache-airflow>=2.11 but this stack is
# pinned to 2.10.2 (1.14.0 = newest release supporting 2.8+).
# openlineage-dbt provides the `dbt-ol` wrapper used by w3c_dbt_marts;
# its latest accepts dbt-core>=1.0.0 so the ==1.8.9 pin holds.
apache-airflow-providers-openlineage>=1.14,<2
openlineage-dbt>=1
