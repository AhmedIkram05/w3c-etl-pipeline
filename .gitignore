# Secrets — never commit
.env

# macOS junk
.DS_Store

# Python bytecode
dags/w3c/__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Airflow runtime files — these are generated, not source
logs/
airflow.db
airflow.cfg
airflow-webserver.pid
standalone_admin_password.txt

# Runtime data folders inside the DAG
dags/w3c/LogFiles/
dags/w3c/StarSchema/

# Empty placeholder folders (keep the dirs with .gitkeep instead)
data/
copilot-instructions.md
.agents

# dbt — build artifacts
airflow/dbt/w3c/target/
airflow/dbt/w3c/dbt_packages/

# Prometheus — runtime data (config is version-controlled separately)
prometheus/data/

# Grafana — runtime data (provisioning config is version-controlled)
grafana/data/
AGENTS.md

# Spark Delta Lake data (generated at runtime)
airflow/spark/delta/*
!airflow/spark/delta/.gitkeep

# Pre-commit
.pre-commit-cache/

# uv local cache
.uv-cache/
